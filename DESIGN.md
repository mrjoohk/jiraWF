# DESIGN — 무엇을 보장하고 무엇을 하지 않는가

## 목표

Jira를 이용한 **개인** 업무 기록·추적 순환. 로컬 작업은 전부 Cowork에서 하고, Jira에는 **변동 사항 업데이트**와 **티켓 등록**만 한다. 특별한 기능은 두지 않는다.

## 설계 원칙 4줄

1. **Jira = 유일한 진실 원천.** 로컬 파일은 전부 파생물이며 지우고 다시 만들 수 있다. 예외는 사람이 직접 쓴 **진행 메모**뿐이다.
2. **Jira에 쓰는 것은 셋뿐** — 코멘트, 티켓, 그리고 **"진행 중"으로의 전이**. 완료 전이는 하지 않는다.
3. **수집·필터·집계는 결정적으로, 문장화·초안은 서브에이전트가.**
4. **Jira에 쓰기 전에는 항상 사람이 본다.**

## 역할 분리

| 단계 | 주체 | 이유 |
|---|---|---|
| 조회 | 메인 에이전트 (MCP) | MCP 도구는 스크립트에서 호출할 수 없다 |
| 원본 저장 | — | 재집계·대조의 근거. 없으면 INV-4가 성립하지 않는다 |
| 필터·집계·정렬 | `aggregate.py` | 입력이 같으면 출력이 같다 |
| 문장화·초안 | 서브에이전트 | 집계 파일만 받으므로 **대화 맥락에서 없는 내용을 끌어올 수 없다** |
| 승인·지시 | 사람 | |

서브에이전트를 쓰는 이유는 컨텍스트 절약이 아니라 **맥락 오염 차단**이다. 일지에서 가장 위험한 결함은 그럴듯한 허위 진술이고, 그것은 대개 세션에 남은 이전 대화에서 나온다.

## 런타임 불변식

자동 검사(`loop_checks.py`). 위반 시 배치는 watermark를 전진시키지 않는다.

| ID | 불변식 | 왜 |
|---|---|---|
| INV-1 | 일일 배치는 **진행 메모 구획을 덮지 않는다** | 메모는 유일하게 재생성 불가능한 원본이다 |
| INV-2 | watermark는 **단조 증가**한다 | 후퇴는 중복 수집 또는 구간 유실을 뜻한다 |
| INV-3 | **승인 기록 없는 push는 존재하지 않는다** | 승인 게이트가 우회되지 않았음을 사후 확인. 파생뿐 아니라 마감이 새로 만든 메인 태스크(`origin: "local"`)도 대상이다 |
| INV-4 | 일지가 언급한 이슈 키는 **원본에 실재**한다 | 서브에이전트의 환각·오타 검출. 사람 눈에 안 띄고, 몇 달 뒤에는 원인 재구성이 불가능하다 |
| INV-5 | 수집 범위가 **그 폴더의 스코프와 일치**한다 | 폴더가 여럿일 때 조회에서 에픽 필터가 빠지면 옆 폴더의 태스크를 중복 추적하고, 마감 시 하위 티켓이 두 번 생긴다. 되돌리기 비용이 큰 쪽이다 |

INV-4는 진행 메모 구획을 검사하지 않는다 — 사람이 쓴 원문이기 때문이다.

## 작업 폴더와 범위

폴더(`WF_HOME`) 하나가 범위 하나를 맡는다. 범위는 **프로젝트 키 + 선택적 에픽 + 선택적 제목 필터**다.

```
WF_HOME_A  →  프로젝트 XXX 전체
WF_HOME_B  →  프로젝트 XXX 중 에픽 XXX-100 만
WF_HOME_C  →  에픽 XXX-100 중 제목이 "[C]"로 시작하는 것만
WF_HOME_D  →  에픽 XXX-100 중 나머지 (제목 "[C]" 제외)
```

에픽 하나를 여러 폴더가 나눠 맡아야 할 때 제목으로 가른다. 판정은 **JQL이 아니라 `aggregate.py`가 정규식으로** 한다. JQL의 `summary ~`는 단어 색인 기반이라 부분 문자열과 다르게 걸리고, 새 이슈나 제목 변경이 색인에 반영되기 전에는 어느 폴더에도 안 잡힌다. 그 실패는 조용하다.

C와 D처럼 **한쪽은 `title_include`, 다른 쪽은 같은 패턴의 `title_exclude`**로 두어야 둘이 합쳐 에픽을 빠짐없이 덮는다. 양쪽 다 `title_include`를 쓰면 어느 패턴에도 안 걸리는 제목이 조용히 사라진다 — 폴더는 서로를 모르므로 이것만은 기계가 못 잡는다. 대신 배치가 매번 "제목 필터로 빠진 키"를 보고해 사람이 대조할 수 있게 한다.

폴더는 서로 완전히 독립이다 — `state.json`, `roots` 지역 번호, watermark를 각자 갖는다. 그래서 폴더를 늘리는 데 비용이 거의 없다.

대신 **범위가 겹치면 안 된다.** 두 폴더가 같은 태스크를 보면 각자 `R-nnn`을 매기고 각자 하위 티켓을 만든다. `worklog-close`의 "이미 `jira_key`가 있으면 다시 만들지 않는다" 규칙은 자기 `state.json`만 보므로 상대가 만든 것을 알지 못한다. INV-5는 이 사고의 앞단(잘못 수집한 시점)에서 멈추게 한다.

플러그인은 **폴더 목록을 갖지 않는다.** 의도한 선택이다 — 전역 레지스트리를 두면 그것이 또 하나의 진실 원천이 되고, 폴더를 옮기거나 지웠을 때 조용히 어긋난다. 대신 실행할 때 폴더를 명시한다.

## 실행 누락에 대한 태도

"전날"이 아니라 **watermark**를 쓴다. 휴가·PC 오프·네트워크 실패로 실행이 빠져도 다음 실행이 그 구간을 흡수한다. 어떤 단계든 실패하면 watermark를 전진시키지 않는다.

## Jira에 없는 일을 티켓으로 만들기

로컬에서 먼저 해버린 일은 Jira에 티켓이 없다. 그대로 두면 일지에도 남지 않고, 관련 없는 태스크의 코멘트로 밀어 넣으면 그 태스크의 이력이 오염된다. 그래서 마감이 **메인 태스크를 새로 만드는 경로**를 갖는다(메모의 `- [신규]` 줄).

**"신규"는 초안 작성자가 아는 범위에서의 신규다.** 초안 작성자에게 주는 집계 파일에는 그 폴더 범위의 내 미완료 태스크만 들어 있다. 에픽 안의 완료된 태스크나 다른 폴더가 맡은 태스크는 보이지 않으므로, 실제로 붙일 곳이 있는지는 마감이 따로 조회해(`merge_targets.py`) **사람이 고른다.** 애매하면 신규로 올리고 사람이 병합으로 돌리는 방향이다 — 억지 병합이 되돌리기 더 어렵다.

세 가지를 지킨다.

1. **승인 없이 만들지 않는다.** 신규 티켓은 되돌리기가 가장 번거롭다 — 삭제 권한이 없으면 사람이 Jira에서 직접 정리해야 한다.
2. **제목이 그 폴더의 범위 안에 들어오는지 만들기 전에 검사한다**(`new_title.py`). 제목 필터로 폴더를 나눠 쓸 때, 접두사 없는 제목으로 만들면 티켓은 Jira에 남고 그 폴더의 일지에서는 사라진다. 만든 사람이 가장 늦게 알아채는 유실이다.
3. **완료로 옮기지 않는다.** 이미 끝난 일이어도 티켓만 만들고 완료 처리는 사람이 한다(원칙 2). 만든 직후 "진행 중"으로 옮기는 것은 `transition_on_update`를 따른다 — 방금 한 일에서 나온 티켓이 "해야 할 일"에 놓이면 보드가 사실과 어긋난다.

## 하지 않는 것

- **자동 구현.** 할 일이 로컬에 등록되면 거기서부터는 사람이 보고 에이전트에 지시한다.
- **할 일 유형 분류.** 사람이 어차피 보고 판단하는 지점 앞의 기계 분류는 아무것도 결정하지 않으면서 틀릴 수는 있다. 과제별 구획은 Jira의 Epic이 이미 제공한다.
- **팀 롤업·대시보드.** 개인 순환이 목표다.
- **양방향 자동 동기화.** 로컬에서의 변경은 마감 명령을 통해서만 Jira로 간다.
- **완료(Done) 전이.** 무엇이 끝났는지는 사람만 안다. "진행 중"으로 옮기는 것은 다르다 — 코멘트가 붙었다는 사실 자체가 근거이고, 틀렸어도 전이 한 번으로 되돌아온다. 그 비대칭이 한쪽만 자동화한 이유다.
- **git 커밋 수집.** 루프는 Jira만으로 닫힌다. 필요해지면 독립적으로 추가할 수 있다.

## 알려진 한계

| 한계 | 영향 |
|---|---|
| 마감을 빼먹은 날은 기록에 남지 않는다 | 배치가 "마감 누락 의심"을 표시하지만 강제할 수는 없다 |
| 버그가 Sub-task라 스프린트 보드에 독립 카드로 안 보인다 | 이슈 타입 기준 버그 지표에서도 빠진다. 의도된 선택 |
| 파생의 파생을 만들 수 없다 | Jira 기본 계층 3단을 전부 소진했다. 메인 태스크 밑으로 평평하게 붙인다 |
| 신규 메인 태스크는 에픽 아래에만 만든다 | `epic_key`가 없는 폴더에서 만들면 과제 미지정으로 남는다. 초안에 표시하지만 막지는 않는다 |
| 병합 후보는 내 담당으로 한정한다 | 남의 태스크에 내 기록을 쌓지 않기 위해서다. 필요하면 사람이 Jira에서 직접 단다 |
| `roots`에 없는 태스크 아래에는 파생을 못 만든다 | 파생 번호를 매길 자리가 없고 일지에 찍을 근거도 없다. 코멘트만 남긴다 |
| MCP 응답 필드 경로가 환경마다 다를 수 있다 | `aggregate.py`가 여러 경로를 시도하고 적중 경로를 `field_paths_used`에 남긴다. **첫 실행에서 반드시 확인할 것** |
| 폴더 목록을 플러그인이 알지 못한다 | 폴더가 둘 이상이면 실행할 때마다 대상 폴더를 지정해야 한다 |
| 에픽 스코프는 `parent` 필드에 기댄다 | team-managed 프로젝트 기준이다. company-managed에서 Epic Link가 `parent`로 안 잡히면 필터를 그 프로젝트에 맞게 바꿔야 한다 |
| 제목 분할은 제목 규약에 기댄다 | 규약을 안 지킨 제목은 어느 폴더도 맡지 않을 수 있다. 폴더끼리 서로를 모르므로 기계가 막지 못한다. 한쪽을 `title_exclude`로 두어 나머지를 받게 하는 것이 유일한 구조적 방어다 |
| 예약 실행은 앱이 동작해야 한다 | 누락된 날은 watermark가 흡수하지만 그날의 알림 자체는 없다 |

## 버전

세 곳에 표기한다: `plugin.json`의 `version` · `state.json`의 `schema_version` · 일지 하단 생성 버전.

`schema_version`이 맞지 않으면 **실행하지 않고 중단**한다. 잘못된 스키마로 계속 도는 것보다 멈추는 편이 복구 가능하다.

이것은 오래도록 문서에만 있는 약속이었고, 실제로는 에이전트가 문서를 지키는 데 의존했다. 지금은 `state.json`을 읽는 모든 스크립트가 `jira_fields.require_schema()`로 직접 막고 종료 코드 2로 끝낸다. **문서가 요구하는 게이트는 기계가 들고 있어야 한다** — 사람이나 에이전트의 준수에 기대는 게이트는 게이트가 아니다.

배포는 태그로 한다. `main`을 따르게 두면 커밋 하나가 예고 없이 전원의 동작을 바꾼다.
