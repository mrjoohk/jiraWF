---
name: worklog-close
description: 오늘 일일 기록의 진행 메모를 읽어 Jira 코멘트와 하위 티켓 초안을 만들고, 사용자 승인을 받은 뒤에만 Jira에 반영한다. 하루 작업을 마칠 때 수동 실행한다. "마감", "오늘 정리", "Jira에 올려줘", "worklog close" 등이 언급되면 사용할 것.
---

# 마감

로컬 진행 메모 → Jira. **이 워크플로우에서 Jira에 쓰는 유일한 지점이다.**

## 절대 규칙

0. **어느 폴더에 대고 마감하는지 먼저 확정한다.** 폴더는 여러 개일 수 있고 각자 다른 범위를 맡는다. 폴더를 잘못 고르면 그 폴더의 `roots` 번호와 `jira_key` 기록이 어긋나고, 중복 티켓으로 이어진다.
1. **승인 없이 push하지 않는다.** 초안을 보여주고 사용자가 명시적으로 승인한 항목만 올린다.
2. **이미 `jira_key`가 있는 항목은 다시 만들지 않는다.** 중복 티켓은 되돌리기 비용이 크다.
3. **push 성공 즉시 `state.json`에 `jira_key`와 `approved_at`을 기록한다.** 기록 전에 다음 항목으로 넘어가지 않는다 — 중간에 끊기면 무엇이 올라갔는지 알 수 없게 된다.
4. 상태 전이(To Do → In Progress 등)는 **자동으로 하지 않는다.** 코멘트만 남긴다. 상태는 사람이 Jira에서 바꾼다.

## 계층 규칙 (Jira 기본 3단을 전부 소진했다)

```
과제(Epic) → 메인 태스크(root, 로컬 R-nnn) → 파생(Sub-task)
```
- **파생은 전부 Sub-task**로 만든다. 문제(BUG)도 Sub-task다.
- Sub-task는 자식을 가질 수 없다. **파생 작업 중 또 나온 항목은 그 파생 밑이 아니라 메인 태스크 밑으로 평평하게 붙인다.** 계보는 코멘트로만 남긴다.
- 버그 표시 방법은 `state.json`의 `issue_type_map.bug_marker`를 따른다:
  - `label:bug` → 표준 Sub-task로 만들고 레이블 `bug` + 요약 접두사 `[BUG]`
  - `type:<이름>` → 해당 sub-task 계층 커스텀 타입으로 생성

## 절차

### 1. 메모 파싱
`daily/{YYMMDD}.md`의 `<!-- MEMO:START -->` ~ `<!-- MEMO:END -->` 구획을 읽는다. 비어 있으면(플레이스홀더만 있으면) 그 사실을 알리고 종료한다.

인식 규약:
- `- [파생] 내용` → 새 Sub-task 후보
- `- [BUG] 내용` → 새 Sub-task 후보(버그 표시)
- 그 외 줄 → 해당 root의 진행 코멘트 재료

### 2. 초안 생성 (서브에이전트)
`jira-drafter` 서브에이전트를 생성한다. 전달하는 것은 **메모 원문 + 오늘 집계 파일**뿐이다.
산출물은 `.workflow/draft.json`:
```json
{ "comments": [ {"local_id":"R-001","jira_key":"XXX-142","body":"..."} ],
  "children": [ {"parent_local_id":"R-001","local_id":"R-001.3","kind":"bug",
                 "summary":"[BUG] ...","description":"..."} ] }
```
`local_id`는 `state.json`에서 다음 번호를 이어 붙인다. **번호는 재사용하지 않는다.**

### 3. 승인
초안을 표로 보여준다 — 무엇이 어느 이슈에 붙는지, 새로 생기는 것이 무엇인지. 사용자가 항목별로 승인/수정/보류할 수 있게 한다. **보류된 항목은 메모에 그대로 남긴다**(다음 마감에 다시 올라온다).

### 4. 반영 (MCP)
승인된 것만, 순서대로:
1. 코멘트 추가
2. Sub-task 생성 → 반환된 키를 즉시 `state.json`에 기록(`jira_key`, `approved_at`)
3. 파생을 만들었으면 **메인 태스크에 그 사실을 코멘트로 남긴다**(보조 기록 — 없어도 `parent`로 추적되지만 일지 가독성에 쓰인다)

### 5. 마무리
- 처리된 메모 줄에 `✓ {jira_key}`를 덧붙인다(삭제하지 않는다 — 그날의 원본 기록이다).
- `logs/{stamp}/evidence.json`에 코멘트 n건·생성 n건·실패 n건을 남긴다.
- 실패한 항목은 **초안에 그대로 두고** 다음 실행에서 재시도 가능하게 한다.
