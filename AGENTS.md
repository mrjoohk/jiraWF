# AGENTS.md — 에이전트용 절차서

**대상 독자: 사용자를 대신해 이 플러그인을 설치·초기화·운영하거나, 이 저장소를 수정하는 에이전트.**

사람이 읽는 매뉴얼은 [README.md](README.md), 설계 배경은 [DESIGN.md](DESIGN.md)에 있습니다. 이 문서는 **무엇을 어떤 순서로 실행하고, 무엇을 보고 멈출 것인가**만 다룹니다.

---

## 0. 이 워크플로우의 위험 지형

절차를 외우기 전에 이것부터. 실수의 비용이 균일하지 않습니다.

| 동작 | 되돌리기 | 태도 |
|---|---|---|
| 일일 배치 (읽기 전용) | 쉬움 — 다시 돌리면 됨 | 막히면 재시도 |
| 로컬 파일 갱신 | 쉬움 — 파생물이며 재생성 가능 | 단 **진행 메모는 예외**, 재생성 불가 |
| Jira 코멘트 | 보통 | 승인 후에만 |
| **Jira 티켓 생성** | **어려움** — 삭제 권한이 없으면 사람이 직접 정리 | 건별 승인, 사전 검사 통과 후에만 |

그래서 **의심스러우면 멈추는 쪽**이 항상 맞습니다. 배치가 멈추면 사용자는 다시 돌리면 되지만, 잘못 만든 티켓은 사람의 시간을 씁니다.

---

## 1. 설치 대행

### 1-1. 사전 조건 확인
```bash
python3 --version          # 3.8+ 필요. 스크립트가 이것으로 호출됨
git --version              # 마켓플레이스 클론에 필요
```

**Windows 주의:** `python3`가 Microsoft Store 스텁이면 아무것도 출력하지 않고 exit 49를 냅니다. `python`은 되는데 `python3`가 안 되는 상태가 흔합니다. 이 경우 스크립트 호출이 전부 실패하므로, `python3`로 가는 셔임을 만들거나 사용자에게 Python 설치를 안내하십시오.

Atlassian 커넥터는 **에이전트가 연결할 수 없습니다.** 사용자가 Claude 앱의 커넥터 설정에서 직접 연결해야 합니다. 지점 조회가 실패하면 여기서 멈추고 안내하십시오 — 이후 단계는 전부 무의미합니다.

### 1-2. 플러그인 설치
대화형 슬래시 명령과 CLI 두 경로가 있습니다. 에이전트는 **CLI를 씁니다.**

```bash
claude plugin marketplace add mrjoohk/jiraWF
claude plugin install jira-worklog@jiraWF
```

### 1-3. 설치 경로 확인
스킬 문서의 `<plugin>`은 다음으로 해석합니다.

```bash
claude plugin list                       # 버전 확인
# 정확한 경로:
python3 -c "import json;d=json.load(open('$HOME/.claude/plugins/installed_plugins.json',encoding='utf-8'));print(d['plugins']['jira-worklog@jiraWF'][0]['installPath'])"
```

경로는 `~/.claude/plugins/cache/jiraWF/jira-worklog/<버전>/` 형태이며 **버전마다 별도 디렉터리**입니다. 업데이트해도 옛 디렉터리가 남으므로, 경로를 하드코딩하지 말고 매번 조회하십시오.

### 1-4. 초기화
`/wf-init`을 **작업 폴더마다 한 번씩** 실행합니다. 명령 자체가 8단계 절차를 담고 있으므로 그대로 따르되, 아래 판단 지점만 주의하십시오.

---

## 2. `wf-init` 대행 시 판단 지점

### 작업 폴더 — 거부해야 하는 경우
- **플러그인 저장소 트리 안**이면 거부합니다. 저장소가 public이라 언젠가 업무 기록이 커밋됩니다.
- 사용자가 다른 git 저장소 안을 지정하는 것은 규칙상 막히지 않습니다. 진행하되 **원격이 붙어 있는지 확인하고 알리십시오.** `.gitignore`에 항목을 추가하되 **기존 `.gitignore`를 덮어쓰지 말고 append**하십시오.

### 범위 — 겹침 확인
같은 프로젝트를 이미 다른 폴더가 쓰고 있다면 에픽 지정은 선택이 아닙니다. 플러그인은 폴더 목록을 갖지 않으므로 **에이전트가 사용자에게 물어 확인**해야 합니다. 겹친 채로 마감하면 같은 태스크에 하위 티켓이 두 번 생깁니다.

### 제목 규약 — 검증까지 할 것
제목 필터를 설정했으면 접두사도 함께 받고, **그 자리에서 실제로 통과하는지 확인**하십시오.
```bash
python3 <plugin>/scripts/new_title.py --state <새 state.json> --summary "확인용 제목"
```
- exit 0 → 확정 제목이 stdout에 나옴
- exit 2 → 접두사가 없거나 필터와 안 맞음. 이 상태로 두면 **나중에 만든 티켓이 사용자 일지에서 사라집니다.**

두 폴더가 각각 "포함" 조건만 쓰면 어느 쪽에도 안 걸리는 제목이 유실됩니다. 한쪽을 "제외"로 두라고 **권고하되, 사용자가 알고 선택했다면 그대로 진행**하십시오. 다만 그 결과(마커 없는 기존 태스크는 어디에도 안 나온다)를 반드시 알리십시오.

### 마이그레이션
기존 `state.json`이 있으면 `project_key`·`epic_key`·`epic_name`·`title_include`·`title_exclude`·`title_prefix`·`issue_type_map`만 갱신하고 **`watermark`와 `roots`는 절대 건드리지 마십시오.** 초기화가 데이터를 지우면 "이상할 때 다시 돌린다"는 가장 자연스러운 복구 행동이 사고가 됩니다.

---

## 3. 운영 — 스크립트 호출과 종료 코드

스킬 문서가 절차의 정본입니다. 여기서는 **종료 코드의 의미**만 정리합니다.

| 스크립트 | 역할 | 종료 코드 |
|---|---|---|
| `aggregate.py` | 원본 → 집계 (결정적) | 0 정상 / **2 설정 오류(정규식)** |
| `render_daily.py` | 집계 → 일일 파일 | 0 정상 |
| `loop_checks.py` | 불변식 INV-1~5 | 0 통과 / **1 위반** |
| `new_title.py` | 새 티켓 제목 확정 | 0 통과(제목 출력) / **2 이 폴더가 못 맡는 제목** |
| `merge_targets.py` | 병합 후보 목록 | 0 정상 |

**모든 스크립트는 `state.json`의 `schema_version`을 먼저 검사하고, 다르면 아무 일도 하지 않고 2로 끝냅니다.** 1(검사 위반)과 2(설정이 깨짐)는 대처가 다릅니다 — 1은 고치고 다시 돌리면 되지만, 2는 `/wf-init`으로 마이그레이션하기 전에는 무엇을 해도 소용없습니다.

### 반드시 지킬 것

**`loop_checks.py`가 1을 내면 watermark를 전진시키지 않습니다.** 다음 실행이 그 구간을 다시 가져가게 두는 것이 복구 경로입니다. 위반 내용은 **가공하지 말고 그대로** 사용자에게 보이십시오.

**`aggregate.py` 출력의 경고를 삼키지 마십시오.**
- `스코프 밖 N건을 제외했다` → 조회 JQL이 폴더 범위와 어긋났습니다. JQL을 고치고 다시 조회하십시오.
- `제목 필터로 N건이 이 폴더에서 빠졌다` → 위반이 아니라 정상 동작이지만, **그 키 목록은 사용자에게 전달**하십시오. 어느 폴더도 맡지 않은 태스크가 생겼는지는 사람만 판단할 수 있습니다.

**첫 실행에서는 `field_paths_used`를 확인하십시오.** `issue_list`가 `<none>`이면 응답에서 이슈 배열을 아예 못 찾은 것입니다. 이때 집계 0건을 "변동 없음"으로 읽으면 안 됩니다 — 조용한 실패이며 일지에 거짓이 기록됩니다.

### 절대 하지 말 것

1. **진행 메모 구획을 직접 편집하지 않습니다.** 파일 갱신은 반드시 `render_daily.py`를 통합니다. 메모는 이 워크플로우에서 유일하게 재생성 불가능한 원본입니다.
2. **일지 본문을 메인 세션이 직접 쓰지 않습니다.** `worklog-writer` 서브에이전트에게 **집계 파일 경로만** 넘깁니다. 대화 맥락이 섞이면 원본에 없는 내용이 일지에 들어가고, 그것이 가장 위험한 결함입니다.
3. **건수·분류를 눈대중으로 세지 않습니다.** 집계는 스크립트가 합니다.
4. **승인 없이 Jira에 쓰지 않습니다.** 초안을 보여주고 사용자가 항목별로 고르게 합니다.
5. **상태 전이를 자동으로 하지 않습니다.** 이미 끝난 일을 티켓으로 만들 때도 만들어만 두고 완료 처리는 사람이 합니다.

---

## 4. 마감 대행 — 순서가 중요합니다

되돌리기 비싼 단계이므로 스킬의 0단계 사전 확인(폴더·스키마·스코프)을 **건너뛰지 마십시오.** `INV-5`는 일일 배치에서만 검사되므로, 잘못 수집된 집계로 마감하면 검사 없이 중복 티켓이 생깁니다.

반영 순서:

1. **병합으로 바뀐 항목** — 만들지 않고 코멘트로 돌립니다
2. **신규 메인 태스크** — 승인 즉시 `state.json`에 기록(`origin: "local"`, `approved_at`) → 생성 → 반환된 키를 즉시 채움
3. **코멘트**
4. **파생(Sub-task)** — 부모가 이번에 만든 태스크면 2에서 받은 키를 씁니다

**기록을 나중으로 미루지 마십시오.** 중간에 끊기면 무엇이 올라갔는지 알 수 없게 됩니다. `approved_at`은 있고 `jira_key`가 `null`인 상태는 **정상적인 복구 대기 상태**입니다 — 다음 마감이 재시도합니다. `INV-3`이 잡는 것은 그 반대(승인 기록 없는 push)입니다.

---

## 5. 이 저장소를 수정할 때

### 5-1. 반드시 통과시킬 것
```bash
bash tests/run_tests.sh                              # 회귀 시험 (음성 대조 포함)
python3 tools/skill_sync_check.py                    # 문서 ↔ 코드 대조
python3 -m compileall -q plugins/jira-worklog/scripts
```

셋 다 CI(`guard.yml`)와 `tools/sync-local.sh`가 자동으로 돌립니다. 손으로 돌리는 것은 빨리 확인하고 싶을 때뿐이고, **기억에 의존하는 검사는 언젠가 안 돌아간다**는 전제로 배치했습니다.

**Windows에서는 `python3` 셔임이 필요합니다**(1-1 참고). 셔임 없이 돌리면 전부 FAIL로 나오는데 원인은 코드가 아닙니다.

### 5-2. CI 가드 — 커밋 전에 로컬에서 확인
이 저장소는 public입니다. `.github/workflows/guard.yml`이 두 가지를 막습니다.

- **업무 데이터 경로**: `daily/`, `weekly/`, `logs/`, `.workflow/`, `state.json`, `jira_project_key_id.txt`
- **실제 Jira 이슈 키**: 허용 접두사는 `XXX`·`INV`·`UTF`뿐입니다

**문서·테스트의 예시는 반드시 `XXX-000` 형식**을 쓰십시오. 실제 프로젝트 키나 사내 도메인이 들어가지 않게 커밋 전에 스테이징된 diff를 직접 확인하십시오.

### 5-3. 버전은 세 곳
```
plugins/jira-worklog/.claude-plugin/plugin.json   "version"
.claude-plugin/marketplace.json                   plugins[0].version
plugins/jira-worklog/scripts/render_daily.py      --version 기본값
```
`claude plugin validate <path>`와 `claude plugin tag`가 불일치를 잡아 줍니다.

### 5-4. `schema_version`을 올릴 기준
`state.json`에 필드를 추가할 때 자동으로 올리지 마십시오. 판단 기준은 하나입니다 — **구버전 플러그인이 신버전 state를 읽었을 때 조용히 틀린 일을 하는가.**

- 한다 → 올립니다. 버전 불일치로 **멈추는 편이 복구 가능**합니다.
- 안 한다(그 필드를 쓰는 기능 자체가 구버전에 없다) → 올리지 않습니다. 불필요한 마이그레이션은 사용자에게 비용입니다.

올렸다면 `wf-init`의 마이그레이션 절차와 `worklog-daily`·`worklog-close`의 기대 버전을 함께 고치십시오.

### 5-5. 반영과 배포 — 두 경로는 별개입니다

**마켓플레이스 소스가 무엇인지 먼저 확인하십시오.**
```bash
claude plugin marketplace list        # Source: GitHub(...) 인지 Directory(...) 인지
```

#### 소스가 로컬 디렉터리일 때 (개발 기기)
`git push`는 **이 기기의 설치와 무관합니다.** 반영은 다음 하나로 끝냅니다.

```bash
tools/sync-local.sh                   # 패치 버전 +1 → 회귀 시험 → 반영
tools/sync-local.sh --version 0.6.0   # 버전을 지정
```

스크립트가 하는 일과, 왜 그렇게 하는지:

- **버전을 올립니다.** 설치는 원본을 직접 물지 않고 `cache/<마켓>/<플러그인>/<버전>/`으로 복사합니다. 버전이 그대로면 `plugin update`는 `already at the latest version`이라며 아무것도 하지 않습니다. 세 곳(5-3)을 한꺼번에 올립니다.
- **회귀 시험을 먼저 돌리고, 실패하면 반영하지 않습니다.** GitHub를 거칠 때는 push라는 관문이 있었지만 로컬 소스에는 없습니다. 커밋하지 않은 중간 상태도 그대로 전역에 나가므로, 그 자리를 시험이 대신합니다. `--skip-tests`는 있지만 급할 때만 쓰십시오.
- **`python3` 셔임을 알아서 만듭니다**(5-1의 Windows 문제).

#### 소스가 GitHub일 때 (배포)
```bash
git push origin main
claude plugin marketplace update jiraWF
claude plugin update jira-worklog@jiraWF     # 마켓플레이스 한정자 필수
```

`claude plugin update jira-worklog`(한정자 없음)는 `Plugin not found`로 실패합니다.

#### 소스 전환
같은 이름으로 다시 add하면 **덮어써집니다.** 제거하지 마십시오 — 제거하면 설치된 플러그인이 고아가 됩니다.
```bash
claude plugin marketplace add /path/to/jiraWF        # 로컬로
claude plugin marketplace add mrjoohk/jiraWF         # GitHub로 되돌리기
```
설치 스코프(`user` = 전역)는 소스를 바꿔도 유지됩니다.

#### 공통
업데이트 후 **"Restart to apply changes"**가 뜹니다. 재시작 전까지 실행 중인 세션은 이전 버전을 씁니다. 급하면 현재 설치 경로에 파일을 직접 복사해 그 세션만 먼저 새 동작을 쓰게 할 수 있지만, 그것은 임시 덮어쓰기이며 다음 업데이트에 되돌아갑니다.

### 5-6. `skill_sync_check.py` — 문서와 코드가 어긋나는 것을 막습니다
스킬 문서는 스크립트의 인자·필드·섹션 제목을 **문자열로 인용**합니다. 그 인용이 코드와 어긋나면 실행 시점에야 드러나고, 그때는 배치가 죽거나 조용히 틀립니다. 이 검사가 양쪽을 대조합니다.

계약을 하나 추가하면(새 인자, 새 state 필드, 새 draft.json 키) **이 검사에도 항목을 추가하십시오.** 안 하면 검사는 초록인데 새 표면은 무방비인 상태가 됩니다 — 초록이 주는 거짓 안심이 검사가 아예 없는 것보다 나쁠 수 있습니다.

항목을 쓸 때는 **"고친 것을 되돌리면 실패하는가"**를 실제로 확인하십시오. 예를 들어 `"require_schema" in agg`처럼 import만 보면, 호출을 지워도 import가 남아 검사가 통과합니다(2026-08-27에 실제로 그렇게 새어 나갔습니다). 호출부를 보아야 합니다.

### 5-7. 테스트를 추가할 때
`tests/run_tests.sh`의 형태는 **"되돌리면 실패하는 음성 대조"**입니다. 통과만 확인하는 시험은 검사가 무력화돼도 계속 통과하므로 아무것도 증명하지 못합니다. 검사를 하나 추가하면 **그 검사를 깨뜨리는 입력**도 함께 추가하십시오.

Windows에서 인라인 python을 쓸 때는 `open(..., encoding='utf-8')`을 명시하십시오. 생략하면 cp949로 읽어 한글 fixture에서 깨집니다.
